<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.hcsc.generic</groupId>
    <artifactId>data-ingestion-framework</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>ingestion-config-gen</artifactId>
  <name>Ingestion Configuration Generator</name>
  <description>Interactive CLI wizard that generates and validates feed configurations
    (HOCON/JSON/YAML) by reusing the framework's own config parsers</description>

  <dependencies>
    <dependency>
      <groupId>com.hcsc.generic</groupId>
      <artifactId>ingestion-core</artifactId>
    </dependency>
    <!-- Reused for dry-run validation, SQL preview and connectivity tests.
         The JDBC classes touched (JdbcSourceConfig, QueryBuilder,
         JdbcHealthCheck) never load Spark classes, so the wizard runs on a
         plain JVM; schema-contract validation needs Spark types and degrades
         to a warning when they are absent (see DryRunValidator). -->
    <dependency>
      <groupId>com.hcsc.generic</groupId>
      <artifactId>ingestion-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.spark</groupId>
      <artifactId>spark-sql_${scala.binary.version}</artifactId>
    </dependency>
    <dependency>
      <groupId>org.scala-lang</groupId>
      <artifactId>scala-library</artifactId>
    </dependency>
    <dependency>
      <groupId>com.typesafe</groupId>
      <artifactId>config</artifactId>
    </dependency>
    <dependency>
      <groupId>org.scalatest</groupId>
      <artifactId>scalatest_${scala.binary.version}</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>2.2.224</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>net.alchim31.maven</groupId>
        <artifactId>scala-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.scalatest</groupId>
        <artifactId>scalatest-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
